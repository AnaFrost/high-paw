import { Footer } from '@widgets/Footer';
import { Header } from '@widgets/Header';
import { AppEl } from './App.styled';

export const App = () => {
	return (
		<AppEl>
			<Header />
			<Footer />
		</AppEl>
	);
};
